name: CI

on: [push]

jobs:
  build:    
    
    runs-on: ${{ matrix.os }}
    
    strategy:
      matrix:
        os: [ubuntu-latest, macOS-latest, windows-latest]
        include:
          - os: ubuntu-latest
            extra_path: ~/.local/bin/conan
            
    env:
      PATH: ${{ matrix.extra_path }}        
    

    steps:
    - uses: actions/checkout@v1
    - name: install dependencies
      run: |
        sudo pip install conan
        mkdir build
        cd build
        conan install ..
    - name: configure
      run: |
        cd build
        cmake ..
    - name: make
      run: |
        cd build
        cmake --build .
    - name: deploy
      uses: actions/upload-artifact@v1
      with:
        name: bin
        path: build/bin
        
